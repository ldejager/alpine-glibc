services:
  - docker

install:
  - docker build -t ldejager/alpine-glibc-test .
  - docker run -d --name alpine-glibc-test ldejager/alpine-glibc-test

script:
  - docker ps | grep -q alpine-glibc-test